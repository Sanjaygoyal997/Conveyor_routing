// Runtime settings, editable on the server without rebuilding.
// apiBase: where ConveyorRouting.Api runs. Leave "" when the API serves this UI itself (same site).
// Example for a separate IIS site: apiBase: "http://10.200.233.14:5080"  (no trailing slash)
window.APP_CONFIG = {
  apiBase: "",
};
